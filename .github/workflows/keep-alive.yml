name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 2 */6 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping health endpoint
        run: |
          curl -X GET https://pelumi-music-9mat.vercel.app/api/songs
